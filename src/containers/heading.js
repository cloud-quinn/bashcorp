import Heading from '../components/Heading'

const mapStateToProps = (state) => {
	return {}
}

const mapDispatchToProps = (dispatch) => {
	return {}
}

const PageHeading = connect(
	mapStateToProps,
	mapDispatchToProps
)(Heading)

export default PageHeading